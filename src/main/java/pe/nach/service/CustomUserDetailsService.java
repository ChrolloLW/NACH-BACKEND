package pe.nach.service;

// Legacy CustomUserDetailsService disabled to avoid bean conflicts with the canonical implementation
// located in pe.nach.infrastructure.security.CustomUserDetailsService.
// The legacy implementation references old domain model classes and is retained for reference only.
